---
import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";

const projets = await getCollection("projets");
---

<Layout title="Mes expériences - Hadrien Poinseaux">

  <section class="min-h-screen w-full flex justify-center items-start p-8 bg-gradient-to-br from-blue-900 via-green-500 to-green-200">
    <div class="max-w-6xl w-full mx-auto bg-white bg-opacity-95 p-10 rounded-2xl shadow-xl">
      <h1 class="text-4xl font-bold text-gray-900 mb-6 text-center">💼 Mes expériences professionnelles</h1>

      <p class="mb-8 text-gray-700 text-lg max-w-4xl mx-auto text-center italic">
        Missions pour <strong>startups innovantes</strong>, <strong>grands groupes</strong> et <strong>associations reconnues</strong>.  
        Ex : une entreprise passée sur <em>« Qui veut être mon associé ? »</em>, et un des plus grands distributeurs de CD/DVD en France.
      </p>

      <!-- CARROUSEL HORIZONTAL MANUEL -->
      <div id="carousel" class="flex gap-6 overflow-x-auto snap-x snap-mandatory scroll-smooth pb-4">
        {projets.map(({ data }, i) => (
          <a href={`/projets/${data.slug}`} 
             class="group relative flex-shrink-0 w-96 snap-center rounded-2xl overflow-hidden shadow-lg border border-gray-200 transform transition hover:scale-105"
             data-aos={i % 2 === 0 ? "fade-right" : "fade-left"} 
             data-aos-delay={i * 150} 
             data-aos-once="true">

            <!-- Image avec zoom -->
            <img src={data.images[0]} alt={data.title} loading="lazy"
                 class="w-full h-72 object-cover transition-transform duration-500 transform group-hover:scale-110" />

            <!-- Overlay texte au hover -->
            <div class="absolute inset-0 bg-black bg-opacity-50 opacity-0 group-hover:opacity-100 transition-opacity duration-500 flex flex-col justify-center items-center text-center p-6">
              <h2 class="text-xl font-bold text-white mb-2">{data.title}</h2>
              <p class="text-gray-200 text-sm line-clamp-4">{data.description}</p>
            </div>

            <!-- Badges techno -->
            <div class="p-4 flex flex-wrap gap-2">
              {data.technos.map(t => (
                <span class="px-3 py-1 bg-green-100 text-green-800 text-sm rounded-full">{t}</span>
              ))}
            </div>
          </a>
        ))}
      </div>

      <!-- Navigation classique -->
      <div class="mt-12 flex flex-wrap justify-center gap-6">
        <a href="/" class="px-6 py-3 bg-gray-200 text-gray-800 rounded shadow hover:bg-gray-300 transition">🏠 Mon portrait</a>
        <a href="/infos" class="px-6 py-3 bg-green-600 text-white rounded shadow hover:bg-green-700 transition">📬 Contact</a>
      </div>
    </div>
  </section>

  <!-- Scripts -->
  <script src="https://unpkg.com/aos@2.3.4/dist/aos.js"></script>
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      if (typeof AOS !== "undefined") {
        AOS.init({ duration: 800, once: true });
      }
    });
  </script>

</Layout>
